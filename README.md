# CRUMB / BLOOM

A standalone, scroll-based React portfolio with video-led case studies, subtle 3D tilt, motion, and responsive layouts.

## Run

```bash
npm install
npm run dev
```

Then open the local URL from Vite.

All media is local in `public/media`. Replace the placeholder email in `src/main.jsx` when ready.
